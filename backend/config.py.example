# This is an example configuration file.
# Copy this file to config.py and fill in your actual credentials.

# AWS Settings
AWS_PROFILES = {
    "dev": "your-dev-profile",
    "gateway": "your-gateway-profile",
}

DYNAMODB_TABLES = {
    "refurb": "your-refurb-table-name",
    "device_registration": "your-device-registration-table-name",
    "pat_labels": "your-pat-labels-table-name",
}

S3_BUCKETS = {
    "support_bucket": "your-support-s3-bucket-name",
}

# Application Settings
HEARTBEAT_MAX_SEARCH_DAYS = 31

# Account to AWS Profile Mapping
# This dictionary maps your application's account IDs to the specific AWS profile
# that should be used for that account. The profile names must exist in your
# server's ~/.aws/credentials and/or ~/.aws/config files.
ACCOUNT_TO_PROFILE_MAPPING = {
    "664721165739": "analytics_profile",
    "272810408402": "atlanta_profile",
    "087946666657": "acorn_profile",
    # "account_id_1": "profile_name_1",
    # "account_id_2": "profile_name_2",
}
